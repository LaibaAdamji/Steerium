# Frontend environment — copy to frontend/.env (optional).
# Leave VITE_API_URL empty for local dev and same-origin Vercel deploys:
# the Vite dev server proxies /api to localhost:8000, and the Vercel
# deployment serves frontend + API on one domain.
# Set it only when the API lives on a different domain than the frontend.
VITE_API_URL=
