# ─── API base URL ───────────────────────────────────────────────────────────
# Leave blank in dev — Vite proxies /api to localhost:8000 via vite.config.ts.
# Set to your deployed backend URL in production (e.g. https://api.steerium.app).
VITE_API_URL=
