# Runtime dependencies for the Vercel serverless API (api/index.py).
# Mirrors backend/requirements.txt minus dev/test tooling (uvicorn runs the
# app locally; Vercel invokes the ASGI app directly) — keep both files in sync.
fastapi==0.115.0
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
psycopg[binary]==3.2.2
pgvector==0.3.5
openai==3.6.0
alembic==1.13.2
pymupdf==1.24.9
python-multipart==0.0.12
python-dotenv==1.0.1
bcrypt==4.2.0
itsdangerous==2.2.0
email-validator==2.2.0
